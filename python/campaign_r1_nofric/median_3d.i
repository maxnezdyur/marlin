# 3-D Taylor impact at the calibrated posterior median (campaign_r1_nofric).
# Wraps the production input; the referenced NEML2 model file already carries
# the calibrated constants from g2_median.json.
#
# Run from this directory:
#   ../../marlin-opt -i median_3d.i

!include ../../examples/impact/3d_slug_thermal_mult_ri.i
